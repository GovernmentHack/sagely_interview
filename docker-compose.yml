version: "3.7"
services:
  backend:
    build: ./packages/server
    ports:
      - "3000:3000"
    image: backend
  frontend:
    build: ./packages/frontend
    ports:
      - "3001:3001"
    image: frontend
